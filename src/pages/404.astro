---
import Layout from '../layouts/Layout.astro';
---

<Layout title="404 - 页面未找到">
  <main class="container mx-auto px-4 min-h-[80vh] flex items-center justify-center">
    <div class="max-w-2xl mx-auto text-center relative">
      {/* 宇航员动画 */}
      <div class="astronaut-container absolute w-full h-full">
        <div class="astronaut">
          <div class="astronaut-string"></div>
          <div class="astronaut-helmet">
            <div class="astronaut-glass"></div>
          </div>
          <div class="astronaut-body">
            <div class="astronaut-arm-left"></div>
            <div class="astronaut-arm-right"></div>
            <div class="astronaut-leg-left"></div>
            <div class="astronaut-leg-right"></div>
          </div>
        </div>
      </div>

      {/* 404文字 */}
      <h1 class="text-[12rem] font-bold leading-none select-none">
        <span class="inline-block transform hover:scale-110 transition-transform cursor-default text-blue-600 dark:text-blue-400">4</span>
        <span class="inline-block transform hover:scale-110 transition-transform cursor-default text-purple-600 dark:text-purple-400">0</span>
        <span class="inline-block transform hover:scale-110 transition-transform cursor-default text-blue-600 dark:text-blue-400">4</span>
      </h1>
      
      <div class="mt-8 space-y-4 relative z-10">
        <h2 class="text-3xl font-bold">这个页面很快就会搭建好</h2>
        <p class="text-gray-600 dark:text-gray-400">
          谢谢你对我这么感兴趣，先看看其他内容吧。
        </p>
        <p class="text-sm text-gray-500 dark:text-gray-400 animate-pulse">
          <span id="countdown">2</span> 秒后躺下做梦...
        </p>
      </div>

      <div class="mt-12 space-y-4">
        <a
          href="/"
          class="inline-flex items-center px-8 py-4 text-lg font-medium text-white bg-gradient-to-r from-blue-600 to-purple-600 rounded-full hover:from-blue-700 hover:to-purple-700 dark:from-blue-500 dark:to-purple-500 dark:hover:from-blue-600 dark:hover:to-purple-600 transition-all transform hover:scale-105 hover:shadow-lg"
        >
          <span class="mr-2">💭</span>
          回到真实梦
        </a>
      </div>
    </div>
  </main>
</Layout>

<style>
  .astronaut-container {
    pointer-events: none;
    opacity: 0.3;
    z-index: 1;
  }

  .astronaut {
    position: absolute;
    width: 140px;
    height: 140px;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    animation: float 6s ease-in-out infinite;
  }

  .astronaut-string {
    position: absolute;
    width: 2px;
    height: 100px;
    background: #cbd5e1;
    top: -100px;
    left: 50%;
    transform: translateX(-50%);
  }

  .astronaut-helmet {
    position: absolute;
    width: 60px;
    height: 60px;
    background: #e2e8f0;
    border: 4px solid #94a3b8;
    border-radius: 50%;
    left: 40px;
    top: 0;
    overflow: hidden;
  }

  .astronaut-glass {
    position: absolute;
    width: 70%;
    height: 70%;
    background: rgba(148, 163, 184, 0.2);
    border-radius: 50%;
    top: 15%;
    left: 15%;
    transform: rotate(-15deg);
  }

  .astronaut-body {
    position: absolute;
    width: 70px;
    height: 80px;
    background: #e2e8f0;
    border: 4px solid #94a3b8;
    border-radius: 40px;
    left: 35px;
    top: 50px;
    animation: pulse 2s ease-in-out infinite;
  }

  .astronaut-arm-left,
  .astronaut-arm-right {
    position: absolute;
    width: 25px;
    height: 40px;
    background: #e2e8f0;
    border: 4px solid #94a3b8;
    border-radius: 20px;
  }

  .astronaut-arm-left {
    left: -15px;
    top: 10px;
    transform: rotate(-20deg);
    animation: wave 2s ease-in-out infinite;
  }

  .astronaut-arm-right {
    right: -15px;
    top: 10px;
    transform: rotate(20deg);
  }

  .astronaut-leg-left,
  .astronaut-leg-right {
    position: absolute;
    width: 25px;
    height: 40px;
    background: #e2e8f0;
    border: 4px solid #94a3b8;
    border-radius: 20px;
    bottom: -30px;
  }

  .astronaut-leg-left {
    left: 10px;
    transform: rotate(10deg);
  }

  .astronaut-leg-right {
    right: 10px;
    transform: rotate(-10deg);
  }

  @keyframes float {
    0%, 100% {
      transform: translate(-50%, -50%) translateY(0) rotate(3deg);
    }
    50% {
      transform: translate(-50%, -50%) translateY(-20px) rotate(-3deg);
    }
  }

  @keyframes pulse {
    0%, 100% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.05);
    }
  }

  @keyframes wave {
    0%, 100% {
      transform: rotate(-20deg);
    }
    50% {
      transform: rotate(-40deg);
    }
  }

  @media (prefers-color-scheme: dark) {
    .astronaut-helmet,
    .astronaut-body,
    .astronaut-arm-left,
    .astronaut-arm-right,
    .astronaut-leg-left,
    .astronaut-leg-right {
      background: #1f2937;
      border-color: #4b5563;
    }
    .astronaut-string {
      background: #4b5563;
    }
    .astronaut-glass {
      background: rgba(75, 85, 99, 0.2);
    }
  }
</style>

<script>
  let count = 2;
  const countdownElement = document.getElementById('countdown');
  
  const timer = setInterval(() => {
    count--;
    if (countdownElement) {
      countdownElement.textContent = count.toString();
    }
    if (count <= 0) {
      clearInterval(timer);
      window.location.href = '/';
    }
  }, 1000);
</script> 