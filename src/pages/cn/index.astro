---
import Layout from '../../layouts/Layout.astro';
import HomeHero from '../../components/home/HomeHero.astro';
import HomeLatestVisual from '../../components/home/HomeLatestVisual.astro';
import HomeSections from '../../components/home/HomeSections.astro';
import { getLatestVisual } from '../../utils/visuals';
import {
  getBlogPosts,
  getChaosPosts,
  getNotes,
  getQuestions,
  getTalks,
  getMeditations,
  getTagCounts,
  type Language,
} from '../../utils/i18n';

// Chinese homepage (/cn/ path)
const lang: Language = 'zh';
const latestVisual = getLatestVisual(lang);

const [posts, talks, questions, chaos, notes, meditations, tagCounts] = await Promise.all([
  getBlogPosts(lang),
  getTalks(lang),
  getQuestions(lang),
  getChaosPosts(lang),
  getNotes(lang),
  getMeditations(lang),
  getTagCounts(lang),
]);
---

<Layout title="首页">
  <HomeHero lang={lang} />
  <HomeLatestVisual lang={lang} visual={latestVisual} />
  <HomeSections
    lang={lang}
    posts={posts}
    meditations={meditations}
    talks={talks}
    questions={questions}
    chaos={chaos}
    notes={notes}
    tagCounts={tagCounts}
  />
</Layout>
