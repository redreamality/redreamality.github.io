请为下面这一个章节制作可复用的多语言交互 demo 适配器。

Demo ID：{{DEMO_ID}}
共享步骤 ID：{{STEP_ID}}
语义参考 locale：{{LOCALE}}
章节标题：{{TITLE}}
章节解释：{{BODY}}
图解标题：{{DEMO_TITLE}}

runtime 会在每个语言版本中向 factory 注入同构但内容不同的 `copy`。下面 JSON 仅用于理解字段语义；生成源码必须读取对应 `copy` 字段，不能复制其中任何自然语言常量：

```json
{{COPY_JSON}}
```

目标：让读者通过操控或观察图形，亲自看见章节描述的关系。进入视口后可以自动运行，但必须允许共享 runtime 暂停、重置、离开视口停算，并在 reduced-motion 下提供完整静态状态。

如果使用 Canvas，`resize()` 获得真实尺寸后必须立即绘制完整首帧，并从安全绘图区计算尺寸；不要等待粒子从画布外进入。知识点切换在暂停状态也必须立即重绘。

只输出满足系统契约的 JavaScript 适配器源码。
