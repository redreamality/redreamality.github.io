## GSC 内容修复与机会实施记录

执行时间：2026-09-25 UTC；Australia/Sydney 本地已为 2026-09-26，文件名沿用本次研究目录的本地日期。基线 `0071c2a`，未提交、未推送、未发布。原有首页 `HomeLatestVisual.astro` 与 `e2e/home-visual-size.spec.ts` 修改保留，不属于本任务。

### 本轮交付范围

以既有文章修复和增强为主，没有新增同意图的重复 URL；三语保留原 slug 和发布日期。

| 主题 | 本轮修改 | 内容机会 |
| --- | --- | --- |
| Claude Agent SDK | 修英文截断、日文虚构接口；三语对齐完整示例；保留并明确 SDK 0.1.3 历史基线 | `ClaudeSDKClient query hooks`、生命周期、超时与清理；区分离线契约和实际 CLI/模型行为 |
| WXT | 完整贯穿扩展例子；storage、popup、background/content、构建和 Safari 边界 | 让“如何开发”查询能够完成一个真实小项目，不追逐异常科普曝光峰值 |
| 扩展框架比较 | 修 messaging 自相矛盾、历史/当前功能混用、未测量性能和维护保证 | 保留选型意图，改为有条件的能力比较，不只换年份 |
| BMAD notes | 固定发布版安装、真实目录和工作流；删除伪官方配置与收益/权限保证 | 品牌词提供明确入门入口和产物检查 |
| OpenSpec notes | 三语实质整理定义、适用性、变更流程和文件契约 | 修正前轮归因偏差：品牌词主要落在 notes，CLI/tutorial 由 blog 承接 |
| OpenSpec blog | 完整 proposal/spec/design/tasks；固定 CLI；严格校验与归档 | 让“第一个变更”有可复算过程，不把 proposal 当 spec |
| GitHub Stargazers | 三语权限限制、空列表/403、当前列表与历史数量区别 | 保留入口型答案，纠正公开仓库无条件可列用户的保证 |
| PYTHONPATH | 补中日文 `.env` opt-in/虚拟环境事实；三语同目录和诊断命令 | 保留已有稳定排名定位，完善“为什么不生效”的排查 |
| Buffett 问答 | 删除无来源财富排名、回报暗示和免费浮存金保证；以历史官方资料澄清题意 | 不追逐“今日利率”等错配查询，不新增资产配置建议 |

### 零曝光工作

详见 `docs/research/gsc-zero-impressions-2026-09-26.md`。

- 当前 sitemap 的 324 个 blog/notes/questions 详情 URL：259 个观察到曝光，18 个声明日期晚于统计窗口、实际部署待核对，47 个未返回。
- 47 个中，29 个是迁移到 Chaos 的 noindex 旧入口、11 个标注窗口末日发布、7 个标注更早日期。
- 七个较早日期页面及三个其他样本执行精确 URL 过滤，本窗口均报告零曝光；日文 CORDIS 实际译文加入时间在窗口末端，不能误判为长期无曝光。
- 29 个旧入口已从 sitemap 生成规则中排除，保留兼容跳转和 Chaos 目的页。该修复针对确定的 sitemap/页面声明矛盾，不声称能解释其他页面退位。
- 跳转 E2E 又发现 Chaos 目的页有重复 H1：27 篇旧内容同时保留正文标题与模板标题。仅删除这 27 行重复标题及相邻空行，保留 frontmatter、正文、路由和所有代码；新增全 Chaos 集合的 AST 标题回归。
- 单 URL Google 收录状态未可靠取得，报告保留未知；全站收录汇总不能冒充每个页面的索引检查。

### 示例验证

所有示例验证均在仓库外的隔离目录运行，不修改用户项目配置，不读取密钥，不调用付费模型。

| 项目 | 已取得的验证 | 不代表什么 |
| --- | --- | --- |
| SDK | uv 环境：SDK 0.1.3、MCP 1.18.0、AnyIO 4.11.0；真实包的导入/签名、Python 语法、hook 回调、MCP handler；模拟传输覆盖两轮响应、hooks、流式输入、缺失结果、超时、错误和 cleanup | 没有验证实际 Claude CLI 权限、真实模型、账单、网络或 OS 子进程可靠性；不推荐把历史依赖当作新服务的安全基线 |
| BMAD | `pnpm dlx bmad-method@6.12.0 install --directory . --modules bmm --tools claude-code --yes`；核对真实生成物和本地 workflow renderer | 没有运行真实模型完成应用功能 |
| OpenSpec | 1.13.2 三语提取文件分别 init → new → validate strict → archive → 主 spec validate；两条需求四个场景，未用 `--no-validate` | 结构检查和归档不证明 magic-link 登录已实现；演练中的未完成任务警告保留 |
| PYTHONPATH | uv Python 3.12：`.env` 不被 Python 自动加载；显式绝对 PYTHONPATH 在两个 cwd 都找到本地包 | 没有实际操控 VS Code UI，终端/调试器配置说明另据官方文档 |
| WXT | 隔离项目固定 WXT 0.21.4 / Vite 6.3.6 / TypeScript 5.9.3；pnpm install、typecheck、Chrome/Firefox/Safari build、Firefox zip 成功；三语七个文件与构建例子一致 | 未在浏览器加载扩展、未原生 Safari 打包、未提交商店、未做框架速度比较 |

文章中每一类“已验证”均与实际执行层次一致。SDK 生命周期示例额外处理外部 AnyIO 取消：连接前创建所属作用域，清理时修改该既有作用域的 shielding 和截止时间，既保护正常清理，又在清理持续等待时有界失败。三语示例一致，真实固定版 SDK + 独立模拟 transport 的复判通过；不外推为真实 CLI 进程回收已验证。

### 回归检查

新增结构完整性测试，使用 Astro Markdown 解析器检查三语修复文章的 frontmatter、正文单 H1 约束、围栏闭合及已知 SDK 截断/虚构 API。代码注释中的 `#` 不按 Markdown 标题处理。

新增 GSC 覆盖分类测试：缺失行不得成为零曝光，归一化锚点仅用于判断是否出现、不得叠加指标，拒绝带过滤条件或图表日期不一致的全站报告，识别行数上限，正确解析 XML loc 而非 hreflang。

独立审查后进一步收紧四点：声明日期不再命名为实际发布状态；元数据 ID 与 Astro glob 使用同样的 GitHub slug 规则并尊重显式 slug；拒绝非 Web (text) 范围；比对可见总行数、采集行数和唯一 URL 数，防止截断制造候选。`github-slugger@2.0.0` 作为直接开发依赖加入，避免依赖 pnpm 的传递依赖可见性。

新增/更新聚焦 E2E 覆盖三语旧 URL、canonical、关键代码、章节完整性、内链往返和 sitemap 别名排除。已有共用测试删除 GitHub 的旧错误保证，将 OpenSpec 初始化断言改为实际固定版本。

已有 4321 预览进程不属于本任务，未停止。Playwright 配置允许 `PLAYWRIGHT_PORT`，本轮使用 4337，避免复用原服务。

### 篇幅与审稿边界

主线统一使用 Astro Markdown AST 的正文文本统计，排除 frontmatter、围栏与行内代码；英文数字计入词项，链接地址不计。不同于作者临时脚本的纯字符串计数，以本表为统一口径。

| 内容 | 中文汉字 | 英文词 | 本轮性质 |
| --- | ---: | ---: | --- |
| SDK blog | 4,928 | 3,198 | 完整重构与补全，达到默认目标 |
| WXT blog | 4,560 | 2,821 | 完整可复现教程，达到默认目标 |
| 框架比较 blog | 8,140 | 5,739 | 原有长文的事实纠错，保留深度，不强行压缩 |
| OpenSpec blog | 2,524 | 1,604 | 旧文局部修复与补齐 delta；低于默认新长文目标 |
| PYTHONPATH blog | 2,134 | 1,628 | 旧文三语事实同步与示例一致性；低于默认新长文目标 |
| GitHub blog | 724 | 467 | 原有入口型短文的权限纠错；低于默认新长文目标 |
| BMAD notes | 1,686 | 1,060 | notes，不受正式 blog 长度目标约束 |
| OpenSpec notes | 1,937 | 1,157 | notes，承接明确的品牌词解释需求 |
| Buffett questions | 855 | 498 | 历史问答，不注水成长博客 |

日文均同步对应信息与代码，不把缺译留成可选项。三个旧短 blog 没有为了数字加入无关背景，也未未经批准迁移现有 URL；**发布前须确认是否允许本轮旧文纠错保留短篇幅**，不能把它们列作已达到默认长度的新长文。当前全部保持未发布状态。

### 统一验证状态

- `pnpm build` 成功，712 页面；变更后重建完成，不使用旧 dist 验证。
- `pnpm test:run`：25 个测试文件，170/170 通过。
- 最终聚焦 Playwright：40/40 通过，0 跳过，0 flaky。包含三语内容、旧 URL、canonical、正文互链、sitemap、SDK 离线行为及移动表格键盘滚动。
- 独立审查发现的四项覆盖脚本问题与 SDK 外部取消问题均修复并复判关闭。没有声称当前仍未知的单页 Google 索引状态已完成。
- 全量 E2E 首轮为 140/159 通过，19 项在现有首页、用户首页卡片测试及一个旧标题测试的 `page.goto(..., waitUntil: 'load')` 阶段超时。单线程首页复跑仍超时，不能全部归因于并发。
- 网络探针记录到未完成的 Google Analytics 与 Chatwoot 脚本；在独立诊断页仅 route.abort 这两个域后立即 `load complete`，无待完成请求。这支持外部资源等待机制，不证明所有用户网络都失败。
- 未修改用户首页组件/测试、未关闭生产统计或客服、未把全量结果包装成通过。最后的表格修复已通过单元、聚焦回归和预览复核；未再次重复同一外部请求阻塞的全量运行。
- SDK 三语分别检查 1440px 桌面与 390px 移动视口，共六个截图：全部 200、单 H1，文档宽度等于视口宽度。宽表保留在有本地化名称、可聚焦、可键盘滚动的局部区域内，没有整页横向溢出。
- `git diff --check` 通过；另外检查 18 个本任务未跟踪文件，无尾随空白。原有用户首页修改未回退，所有测试进程结束，GSC 自有标签页租约已释放。
- 本地预览保留在 `http://127.0.0.1:4337/`（PID 71480）；原有 4321 服务未停止。源码、报告与回归测试均未提交或发布。

### 异常与规避

未发现项目 `agent-incidents` 日志及记录工具。以下为本次候选记录，没有追加 AGENTS.md，也没有晋升长期规则。

| 范围 | 症状与根因 | 修正/规避 |
| --- | --- | --- |
| 文件定位 | 猜测 `src/content/config.ts` 不存在；当前为 `src/content.config.ts` | 先 `rg --files`，后读取 |
| rg | 可选搜索无结果返回 1；另一次 Windows 字面通配根 `e2e/*.spec.ts` 返回路径错误 | 无结果显式处理；使用目录加 `-g`，不把 wildcard 当根 |
| GSC | URL 检查填值后没有真实导航，option 查找失败；索引明细导航返回 Chrome 错误页但 shell 退出 0 | 读回 URL/表头/日期，保留索引未知；有限重试后停止，不把数据缺失解释为未收录 |
| Vitest | `fileURLToPath(import.meta.url)` 在变换后不是 file URL | 用仓库 cwd 定位测试夹具；重跑对应测试通过 |
| SDK 依赖 | 只固定旧 SDK 时解析到 MCP 2.2.0，缺少 `Server.list_tools` | 固定兼容 MCP/AnyIO，并运行实际 handler；注明历史依赖不等于推荐新服务采用 |
| SDK 异步清理 | 旧模块 query 在消费中途抛错触发跨 task 清理问题 | 完整消费后报告结果错误；取消/总截止使用明确的客户端生命周期，不宣称 finally 能保证所有部分启动清理 |
| SDK fixture | 临时测试漏模拟字符串输入响应而等待 | 修测试传输并重跑；这是测试夹具缺陷，主动停止退出 1 不算文章原始缺陷 |
| 内容扫描 | 初版扫描把围栏内 `# Windows` 当正文 H1 | 按 Markdown AST 区分 code/heading，不删除注释 |
| 补丁 | 同一 patch 同路径删除/新增以及旧上下文导致拒绝 | 拆小补丁，重读精确区块；拒绝不是部分应用成功 |
| BMAD 资料 | 某旧文档路径 404，tag 下位置已迁移 | 根据版本文件树定位真实 `docs/start/install-bmad.md` |
| OpenSpec 演练 | 首轮 archive 后严格校验失败，delta 缺 Purpose 使主 spec 留占位文案 | 补完整 Purpose，三语重新走全流程，不关验证器 |
| PowerShell | `foreach` 直接接管道解析失败 | 先收集结果，再管道输出 |
| 依赖搜索 | rg 未跟随包链接而返回无匹配 | 读取实际导出路径，不当作接口不存在 |
| WXT 下载 | 安装中 registry ECONNRESET/下载缓慢，pnpm 自动重试成功，最终退出 0 | 保留网络重试证据，不据此更换根依赖或改写文章版本 |
| WXT 发布提示 | Firefox 数据声明与 ID 警告；pnpm 阻止 esbuild 安装脚本但实测构建成功 | 不盲目批准脚本、不隐藏发布要求；文章明确资源构建不等于商店准入 |
| 独立审查 | 初版覆盖脚本把声明日期命名成发布状态、漏匹配混合大小写 ID、未限制搜索类型、未发现分页截断 | 明确声明日期；使用同源 slug 算法；保存并核验 Web (text)；比对总数和去重数，针对性单测 6/6 通过 |
| SDK 外部取消 | 独立真实 SDK + 模拟 transport 验证发现本轮示例外部取消时进入 close 但未完成其异步等待 | 与内部超时分开修复和测试；不把这个 fixture 结果冒称真实 CLI 泄漏已证实 |
| 差异检查 | 临时用 `git -c core.autocrlf=false diff --check` 关闭本仓库既有转换规则，导致 CRLF 被误报为大量行尾空白 | 不修改文件去迎合临时错误口径；恢复普通 `git diff --check` 后退出 0，警告日志放系统临时目录 |
| 聚焦 E2E 首轮 | 21/37 通过，16 项失败：智能排版撇号差异、默认 load 等待超时、正文链接计入相关推荐、spec 目录/文件名分开而断言要求完整路径 | 保留事实断言并归一化排版；等待 DOM；正文限定 `article > div.prose`；三语明确完整 spec 路径；重新构建后复测 |
| 聚焦 E2E 第二轮 | 34/37 通过；三语 Chaos 目的页 H1 实际为 2，不是定位误差 | 源码确认模板与 Markdown 重复标题；修复同模式的 27 篇内容并加全集合 AST 回归，不削弱单 H1 断言 |
| 全量 E2E / 单线程首页复跑 | 19 项等待 load 超时；单线程同样失败；探针发现两个外部脚本未结束 | 独立诊断仅阻断 Google Analytics/Chatwoot 后 load 完成；保留真实失败记录，不改用户测试来掩盖外部依赖 |
| JSON 报告读取 | pnpm 在 JSON 前后输出脚本横幅及 ELIFECYCLE 尾行，直接解析失败 | 先确认文本边界，再解析其中 JSON；后续优先 `pnpm exec playwright` 或独立 reporter 输出文件 |
| 浏览器诊断 | Chromium DNS 映射参数没有可靠阻断目标请求，代理路径原因未确定 | 不将它作为有效对照；使用 Playwright 的精确 route.abort 做可验证的单页对照 |
| 移动预览 | SDK 三列表格使 390px 视口的整页宽度达到 565–671px，两次探针退出 1 | 为三语该表增加局部可聚焦横向滚动区，保留表格语义；加键盘滚动与整页无溢出的移动 E2E |

下载缓慢、依赖弃用、uv `--no-project` 以及 Git LF/CRLF 提示另属警告，不与失败混淆。最终实际测试失败如有，收尾必须继续记录其原因和重跑结果。
